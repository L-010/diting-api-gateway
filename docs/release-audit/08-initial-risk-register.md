# 初始风险登记

评级说明：P0 表示可能立即造成重大生产事故/数据泄露；P1 表示上线前应阻断或必须完成验证；P2 表示重要但可在受控条件下后续验证；P3 表示低影响或文档/治理问题。下列“漏洞”均未在本阶段主动利用，推测项明确标记待验证。

| 编号 | 类别 | 风险描述 | 级别 | 证据 | 触发条件/影响 | 已验证 | 后续验证 | 是否阻断 | 业务确认 |
|---|---|---|---|---|---|---|---|---|---|
| P1-001 | 部署 | 默认后端脚本使用 Uvicorn `--reload` 和开发绑定方式；未发现独立生产启动/进程托管配置。生产误用可能导致不稳定、额外文件监视或不符合可用性要求。 | P1 | `scripts/run-backend.ps1:5-6` | 按现有脚本直接生产部署；影响稳定性、发布和运维可控性 | 已确认脚本事实；生产误用待验证 | 获取生产启动单元、反向代理、容器/守护配置并演练 | 是，直到部署方式确认 | 是 |
| P1-002 | 数据库/发布 | 架构文档记载迁移头 `c3d4e5f6a7b8`，只读 `alembic current` 为 `f9a0b1c2d3e4`，文档与运行基线不一致。可能导致部署人员选错迁移/回滚基线。 | P1 | `docs/ARCHITECTURE.md` 数据库段；命令输出 | 按旧文档部署或回滚；影响数据库一致性 | 差异已验证；实际部署后果待验证 | 对照迁移链、目标环境数据库、发布脚本和回滚演练 | 是，直到基线统一 | 否/需运维确认 |
| P1-003 | 外部依赖/配置 | 当前 `.env` 的 `TOMODD_UPSTREAM_TOKEN` 为空；需要 Bearer 认证的上游调用可能失败。 | P1 | 本次仅记录变量存在性；`config.py:32-33`、`LOCAL_RUNBOOK.md` | 启用需要 Token 的 tomoDD 工具；影响调用可用性或诱发错误处理路径 | 配置为空已确认；调用影响未验证 | 使用专用测试凭证做只读 health/info 与受控调用，禁止记录值 | 是，若 tomoDD 是上线必需依赖 | 是 |
| P1-004 | 权限/业务规则 | 角色模型只有 `user/admin`，但路由策略包含 `owner/shared/admin_only`；未发现组织/租户模型。若业务需要租户隔离，当前代码证据不足以证明跨租户隔离。 | P1 | `models.py:19-22,303-316`、`schemas.py:777-788` | 引入多组织/租户数据；可能导致数据越权 | 未验证漏洞，仅为边界缺失观察 | 提供租户模型和跨租户测试账号，执行矩阵化越权测试 | 是，若产品要求多租户 | 是 |
| P1-005 | 可观测性/扩展 | 限流、监控告警和调用事实表使用 SQLite；架构文档承认生产需 Redis/Prometheus/OpenTelemetry，但代码库未提供迁移方案。横向扩展可能出现限流不一致、锁竞争或指标延迟。 | P1 | `docs/ARCHITECTURE.md:119-123`、`models.py` 限流表、`admin.py` 监控 | 多进程/高并发生产；影响防滥用、SLO 和告警 | 未进行性能/多进程验证 | 明确目标 QPS/并发，使用生产形态数据库和多实例压测 | 是，若目标超出 SQLite 适用范围 | 是 |
| P2-001 | 前端质量 | 未发现前端自动化测试和 E2E；关键注册、审批、Key、文档、文件下载流程没有浏览器证据。 | P2 | `frontend/package.json`、页面清单、测试目录 | 浏览器特定状态/权限/响应式问题；影响用户流程 | 缺口已确认 | 建立 Playwright 流程和多角色测试数据 | 建议阻断关键流程发布 | 否 |
| P2-002 | 生产配置 | `.env.example` 的公网 Gateway 地址、SMTP 等为空模板值；本地 `.env` SMTP 未配置，公开注册/找回按文档关闭。若生产沿用，用户生命周期功能不可用。 | P2 | `.env`/`.env.example` 变量存在性、`config.py:92-95`、README | 生产未配置 SMTP/公网地址；影响注册、邮件与客户端接入 | 配置状态已确认；生产是否沿用待验证 | 使用非生产配置审计和启动时配置清单验证 | 视功能范围阻断 | 是 |
| P2-003 | 数据保留 | 调用记录默认保留 90 天，清理依赖外部计划任务；未发现计划任务部署定义。清理未运行可能积累敏感元数据并影响 SQLite 容量。 | P2 | `maintenance.py:16-42`、`LOCAL_RUNBOOK.md:97-103` | 未配置定时任务；影响容量、隐私和查询性能 | 依赖缺失已确认 | 检查生产计划任务/cron、失败告警和保留策略 | 建议上线前确认 | 是 |
| P2-004 | 日志脱敏 | 代码设计声明日志脱敏，但本阶段未启动服务并验证未处理异常、上游错误和 worker 异常文本是否可能包含敏感值。 | P2 | `main.py:136-156`、`observability.py`、`file_worker.py:67-73` | 特殊异常/第三方响应进入日志；可能泄露凭证或个人数据 | 未验证 | 在隔离测试环境注入敏感样例并检查日志，随后做 secret scan | 建议上线前验证 | 否 |
| P3-001 | 文档治理 | `TEST_REPORT.md` 与 `docs/ARCHITECTURE.md` 的历史状态/迁移头可能滞后于代码；审计人员容易依据过期结果。 | P3 | `TEST_REPORT.md`、`docs/ARCHITECTURE.md` | 复用旧报告；影响决策准确性 | 差异已观察 | 发布前生成带提交/时间/环境的机器可读报告 | 否 | 否 |

没有观察到可直接确认的 P0；P1 项均需要上线前完成验证或确认其不适用。
