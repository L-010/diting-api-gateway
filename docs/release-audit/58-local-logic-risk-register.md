# 第六阶段：本地逻辑风险登记

| 编号 | 风险/发现 | 级别 | 状态 | 本阶段证据与后续条件 |
|---|---|---:|---|---|
| LL-001 | 静态 Gateway 路径可能被参数路径匹配到错误上游。 | P1 | 已修复 | 路由按具体度排序；变异测试与回归通过。 |
| LL-002 | 用户名大小写变体可并发写入，且约束异常可能变成 500。 | P1 | 已修复 | 新增 `username_key` 唯一索引、受控 409、隔离 upgrade/downgrade/upgrade 通过。 |
| LL-003 | SQLite Worker “先读后写”可使同一文件任务被重复领取。 | P1 | 已修复 | 条件更新；双线程屏障只返回一个 lease。 |
| LL-004 | 文件重新出现时保留旧 `deleted_at`，可能被后续清理误处理。 | P2 | 已修复 | 重登记清除墓碑，状态为 ready。 |
| LL-005 | 负 Content-Length 未在服务层拒绝。 | P2 | 已修复 | 400 回归测试通过。 |
| LL-006 | 新增 migration 后 readiness 和本地迁移检查仍引用旧 head。 | P2 | 已修复 | 预期 head 统一为 `a2b3c4d5e6f7`。 |
| LL-007 | 项目无 tenant/organization 模型，无法证明多租户隔离。 | P1 | 业务语义未知 | 当前仅验证 `owner_user_id` 隔离；需先定义租户模型与跨组织规则。 |
| LL-008 | `shared` 对任意有效 Key 放行；admin Key 不绕过 owner Gateway 路由。 | P1 | 业务语义未知 | 这是当前代码行为，是否符合产品需要业务确认。 |
| LL-009 | 告警是实时派生列表，无确认、关闭和历史状态机。 | P2 | 业务语义未知/功能未实现 | 若产品需要告警闭环，先定义状态、操作者、审计、幂等与保留规则。 |
| LL-010 | 平台不执行/取消任务，只同步上游任务与文件元数据。 | P2 | 业务语义未知 | 若需要创建、取消、重试任务状态机，应定义上游契约及补偿规则。 |
| LL-011 | 多进程/多主机 SQLite 和真实网络重试未作容量级验证。 | P1 | 本地未关闭 | 本轮只验证双线程单任务不重复 claim；需隔离多进程实验和明确存储拓扑。 |
| LL-012 | SMTP/上游错误文本在真实供应商场景的脱敏程度未知。 | P2 | 本地未关闭 | Mock 未携带秘密；应在隔离供应商环境验证错误与审计脱敏。 |
| LL-013 | 旧库若已有大小写等价用户名，新唯一 migration 将拒绝升级。 | P2 | 已知迁移前置条件 | 这是防止静默选择账号的保守行为；升级前应在数据库副本解决冲突。 |
| LL-014 | 前端无组件测试，未以浏览器验证卸载、重复点击和移动端交互。 | P2 | 本地未关闭 | 类型、构建与静态审查通过；待具备本地浏览器环境后补 E2E。 |
